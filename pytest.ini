[pytest]
testpaths = tests
addopts = -q
markers =
    slow: runs the whole exercise bank through the grader
    walk: the full learner walkthrough of the interface (opt in with -m walk)
    walk_fast: the part of the walkthrough that finishes in seconds
    walk_full: the exhaustive content sweeps (every exercise, every question)
