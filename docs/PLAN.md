# Operator's Console - audit and improvement plan (started 2026-09-17)

Status words: DONE = on disk and tested · IN PROGRESS = an agent is on it · PENDING = queued · DECISION = needs the owner.
Baseline: main, clean tree, 172 tests green, PySide6 6.x, Python 3.14, PyInstaller builds (setup.exe + portable zip + macOS), install one-liners.

## What the owner asked (verbatim, 2026-09-17)
"analyze it and review and audit it i want it harsh tested improved and the ui improved as well, also when someone starts you just show stuff thats optional and stuff thats extra: for example in this image a lot is talking about github just see the best one that only the person will ever need the rest is hidden under it expandable by an arrow if he needs more stuff to study optional stuff, only do that to the stuff thats actually optional. also improve the ui improve tests and stuff, also when i open a page or a cmd a ton of tabs opened and close small ones as if running a task or a page then closes i want a clean proper experience you can decide plan and improve and research what needs to be improved"

| # | Item | Status | Evidence |
|---|---|---|---|
| 1 | Flashing "small tabs/windows" when opening a page or command | DONE (agent killed 09-17; completed and verified under rows 14 and 29) | suspects: widgets shown before parenting; full teardown + rebuild of every card on each view visit (Scroller.clear/_drop_layout); the Ctrl+K Popup results window; instrument topLevelWidgets during navigation, then fix and pin with a test |
| 2 | Optional study resources: show the single best one, the rest under an expandable arrow; only for genuinely optional groups | DONE (agent killed 09-17; completed and verified under rows 18 and 33) | data: `primary` per resource + `optional` per group (phase "Start here", shelf, channels, field libs; never gates/exercises/projects/steps); Disclosure widget "N more to study", 180 ms ease-out, remembered per group |
| 3 | UI polish (both themes): feel, focus, spacing, pills, contrast, typography, empty states | DONE (superseded by 26, the 09-22 UI rework; see also 22, 30 and 24) | renders before/after under the session scratchpad console/ui |
| 4 | Core harsh audit: storage atomicity/migration/undo, FSRS-6 correctness, planner edge cases, grader child (timeouts, grandchildren, output bombs), updater + installers | DONE (agent killed 09-17; completed and verified under rows 14 and 21) | docs/AUDIT-core.md |
| 5 | Installer/updater integrity: install.ps1 and install.sh download release assets with NO hash or signature check and build.yml publishes no checksums; `irm ... main/install.ps1 | iex` pins to a branch, not a tag | DONE (agent killed 09-17; completed and verified under rows 19 and 20; release gate in 41) | found by manual review 2026-09-17: publish SHA256SUMS from the workflow, verify in both installers and in core/updates.py before applying |
| 6 | Tests: vacuity (mutation), new coverage for 1-5 | DONE (coverage landed with 20, 21, 25 and 10b; over 1,000 tests collected on 09-23) | |
| 6a | Re-running the installer updates in place to the latest release and never touches the learner's progress (data dir separate from the program dir; running instance stopped gracefully; shortcut and settings kept) | DONE (agent killed 09-17; completed and verified under row 20) | owner 09-17: "when people rerun the installer it just updates to latest version and doesnt delete their progress" |
| 6b | In-app update: a button showing the new version; one click downloads, verifies (SHA256SUMS), swaps the build and restarts cleanly into the new version - no console, no cmd, same view reopened | DONE (agent killed 09-17; completed and verified under row 19; old builds under 39) | owner 09-17: "the ui should show a button to update showing the new update and version ... it will restart with the new update no cmds just a clean restart" |
| 6c | Walk the whole app as a learner on every change: onboarding, every view in both themes, all 21 phases/gates, all 96 exercises (reference solutions must pass), all 167 quiz questions, review sessions, all 22 projects, library, search, settings, undo/redo, update button, relaunch; sensors for exceptions, warnings, blocking steps, transient windows, clipped controls | DONE as a standing item (last full walk recorded under row 24; re-run after every batch) | owner 09-17: "keep trying to go through the entire roadmaps ... testing every feature through the ui as a user would"; suite tests/walkthrough (`pytest -m walk`), report docs/WALKTHROUGH.md; standing item - re-run after every fix batch |
| 7 | Independent skeptic pass on every fix (Fable) | DONE (see 20) | |
| 8 | Release: version bump, changelog, build, tag | DECISION (owner tags and publishes; version.py is 1.1.0 and CHANGELOG.md is written, row 43) | |
| 9 | Manual sweep 2026-09-17: README numbers match the data (21 phases, 96 exercises / 430 checks, 167 questions, 22 projects / 130 requirements, 9 tracks, 30 fields, 24 certs, 7 shelves); no build junk committed; CI runs pytest before packaging (`needs: test`) | DONE (verified) | nothing to fix |
| 10 | No lint config (`.ruff_cache` exists, no `[tool.ruff]`, no lint step in CI); screenshots in docs/screenshots dated 09-03 will be stale after the UI work | DONE for lint (row 10b); screenshots re-shot 09-23 under new names, row 43 | |
| 13 | The data bundles are GENERATED by build_tools and the pipeline is reproducible + deterministic (exercises.json and projects.json byte-identical; curriculum.json identical to git HEAD apart from its `generated` date). So any hand-edit to `src/operators_console/data/*.json` is destroyed by the next `python build_tools/transform.py`. The UI agent's `primary`/`optional` flags must be authored in build_tools (transform.py rule or a `resource_picks.json` side file) and the bundle regenerated | DONE (resolved by row 18: flags authored in build_tools/resource_picks.json; pipeline test green) | `pytest -m slow tests/test_content_pipeline.py`: exercises/projects/determinism pass, curriculum fails today with `differs at ['phases','fields','channels','shelf']` - which is precisely the hand-edit drift |
| 14 | 2026-09-22: all three agents were killed by the weekly limit on 09-17 before their final verification. Re-verified by hand: tree compiles, ruff clean; reports in docs/AUDIT-core.md (U1-U10 updater/installer incl. the critical length-only download check, S1-S4 storage, H1-H3 undo, R1 grader grandchildren, P1-P3 planner, F1-F4 FSRS - fixed; F5-F7, X1 left and documented), docs/CHANGES-ui.md (35 stray windows per session -> 0; the disclosure; polish), docs/WALKTHROUGH.md (walk suite; W-01/W-02 navigation freezes) | DONE (verified) | |
| 15 | Projects page: rebuilt all 22 cards (~2,100 widgets) on every visit and every status press (457-537 ms offscreen, 2-3 s on screen); `show_target` never scrolled (and `ensureWidgetVisible` would have cut a tall card's title); notes written + fsynced on every keystroke; a tick left the card's bar still; counter counted 22 while Today counted the plan; the debounce then exposed notes lost on quit and a hide-before-build crash | DONE | built once, synced in place (repeat visit 18 ms, refresh 1 ms); one bulk `Store.project_states()`; off-track projects folded in the Disclosure; counter = Today's; notes debounced 450 ms with an in-memory dirty flag, flushed on hide and in `closeEvent` before the store closes; top-aligned scroll; tests/test_projects_view.py (18) |
| 16 | Test suite took 14 min (baseline ~40 s): the `window` fixture only closed windows - 31 stayed alive (17,394 widgets) after test_ui.py - and each new window's app-wide `setStyleSheet` re-polished all of them (20-26 s setup per test); closed-but-alive windows' timers also fired against closed stores | DONE | fixture destroys window + context (0 widgets left); `MainWindow.apply_theme` skips re-applying an identical stylesheet/font (a production saving too); test_ui.py 273 s -> 14.7 s |
| 17 | Remaining navigation cost: Stats and Roadmap rebuilt everything on every visit (95-136 ms offscreen, ~0.5 s on screen) | DONE | `View.store_unchanged()/mark_drawn()`: a page skips its rebuild while SQLite's `total_changes` (and, for Stats, the date) is where it was - no change signal to forget; tests/test_view_redraw.py (8), mutation-checked both ways (never-skip kills 3, always-skip kills 5). Library first visit 274 ms is a one-off build and stays |
| 18 | Content-pipeline drift flagged 09-17 (hand-edited disclosure flags) | DONE | the flags now come from build_tools/resource_picks.json via transform.py; tests/test_content_pipeline.py 4/4 green |
| 19 | In-app update on the INSTALLED build could never succeed: the helper ran from the install folder, and Windows lets a running exe be renamed but never overwritten - which is what Inno Setup does (probe on the real 1.0.0 build: rename OK, overwrite "Permission denied", python314.dll delete "Access is denied"). The installer failed silently and the old version reopened every time. Also `/SILENT` shows Inno's progress window (docs: only `/VERYSILENT` shows none), and the app reopened on Today, not where the learner was | DONE (verified end to end on a fresh PyInstaller build: old app running -> Qt-less helper copy 0.09 s -> app quits -> checksum verified -> swap -> new version relaunched from the install folder; no rollback folder or package left) | helper now runs from a Qt-less copy in `updates/helper-<pid>` (29 MB, 0.1 s; the copy runs `--version` fine), falls back to in-place; `/VERYSILENT /SP- /LOG=updates/setup.log`; old copies cleared 3 s after start; one-shot `resume_after_update` bookmark (15 min expiry) reopens the page/exercise/phase; tests/test_update_restart.py (15), 7/7 mutants killed; the old test that pinned `/SILENT` corrected |
| 11 | Local `dist/` holds 1.0.0 builds while `version.py` is 1.0.2 (releases come from CI, so this is only a stale local folder); the frozen exe answers `--version` (exit 0) | DONE (verified) | rebuild locally only if a local artefact is wanted |
| 12 | `python -m operators_console` needs an editable install (`pip install -e ".[dev,build]"`) - README line 143 already says so; tests add src to sys.path in conftest | DONE (not a defect) | |

## Owner's request 2026-09-22 (verbatim)
"plan through every edge case test every part of this app improve layout ui add any features or anything that is deemed good to have and then test everything like a user would"

| # | Item | Status | Evidence |
|---|---|---|---|
| 20 | Independent skeptic pass (Fable) over the whole uncommitted diff | DONE - 13 findings, all fixed | (1 HIGH) a repo URL or name typed just before Quit was lost: the field saved on focus-out after the store had closed -> closeEvent takes focus first. (2) the helper copy recursed into a data folder kept inside a portable install -> the data and staging folders are skipped, and a portable update with data inside says so before quitting. (3) a failed or refused update reopened the old version with no word and no button until tomorrow -> `update-failed.json` note, shown once as a toast, and a fresh check that day. (4) pages skipped their redraw after another copy of the app wrote -> SQLite `data_version` joins the redraw key. (5) a refused folder rename fell back to copy-and-delete and gutted `_internal` -> rename or refuse, with retries, never copy. (6) output written past the print capture was held whole by the app (48 MB) -> bounded pipe readers. (7) a refusal relaunched the old build before it had exited -> wait first. (8) FSRS: a NEW card with a stale step crashed on Hard -> step 0. (9) a later backup row missing a column escaped the Import handler -> validated. (10) a rolled-back swap left the new build's extra files -> removed. (11) install.sh downloaded the AppImage before refusing a release with no checksums -> refuses first. (12) `install.ps1 -Portable` rerun deleted the learner's own files kept beside the app -> they move back. (13) `available()` raised on an https->http redirect, and `download_update(dict(offer))` crashed -> None / a plain ValueError. Pinned in tests/test_learner_flow_fixes.py, tests/test_update_restart.py, tests/test_restore_edges.py |
| 21 | Edge-case sweep of the core (29 failing tests, 16 findings; 46 held and are kept as coverage) | DONE - 15 fixed, 1 documented | **Grader:** correct code was marked unreadable or timed out when a `__del__`, an atexit hook or a thread printed after the answer -> the child writes to the real stdout and leaves with `os._exit`, the app parses with `raw_decode`; a learner's `json.py` in the workspace broke every later run -> `-P`; Windows had no memory cap -> a Job Object (1 GiB per process, kill-on-close also sweeps grandchildren); a traceback after `setrecursionlimit(10**6)` took 18 s to format and was reported as an endless loop -> learner frames only, first 3 + last 12, chains kept; tracebacks opened with the grader's own line; a pasted BOM was a syntax error; a frozen Pool worker opened a second copy of the app -> `freeze_support()`; the capture buffer grew until the new memory cap killed the child -> a capped writer. Process pools themselves stay unsupported inside the grader (making them work re-runs the learner's top level in every worker; no exercise uses one) and now say so in words. **Plan:** goal phases the Roadmap lists counted nowhere else (51 of 81 track x goal combinations) -> one plan definition in Progress; the review cap withheld cards failed a minute earlier -> learning steps are exempt; NaN/Infinity in a setting crashed the plan. **Store:** five kinds of damaged backup escaped Import with no message -> each is a ValueError with a sentence; a restore accepted schedule rows and a retention the app then could not read -> refused / clamped, and the app says why instead of failing to open; a snapshot that could not be written failed silently; a BOM backup was refused; deleting a log entry left its study time on the chart and in the streak; the Log form kept yesterday's date past midnight. tests/test_runner_edges.py (19), tests/test_restore_edges.py, tests/test_plan_edges.py, tests/test_edges_held.py (46); all 96 reference solutions still pass |
| 22 | Layout pass with REAL fonts: 19 configurations, both themes, 940x620 to 1920x1080, 150% scale, 1.6x text | DONE - re-measured | Functional: **Practice hid the result and the hint right after every run** (a list refill reloaded the exercise; it also wiped the undo history) and could save pre-restore code over a restored snapshot -> fixed, status pill refreshed separately; **Quiz and Review counted a second press of Check** (a second rating in the schedule; Skip still worked after the answer was seen) -> controls removed on answer, answering is idempotent. Visual: two sidebar items looked selected (focus = active) -> distinct dashed focus, Tab-only; no visible keyboard focus on radios, checked boxes, good/bad buttons, lists, tables, the editor -> rules for each; spin boxes and dates had no arrows (Qt draws QSS triangles as bars) -> arrows drawn in the theme's ink; dark radios invisible (1.2:1) -> styled; section underline never drew; Again was the same red as the suggested Good -> outlined; the 1.6x text setting only grew plain labels -> the whole sheet scales, fixed widths became minimums, the sidebar widens with the text and scrolls when short of height; practice cut off at the minimum window and Run checks below the fold at 768/864 px -> list 190 px and wraps, split 420 px, the result scrolls into view; update label clipped and its fill dropped contrast to 3.7:1 -> shorter label, bar under the words; search popup crooked and scrolling sideways -> title first, elided; form columns aligned; drawn status icons in the practice list. Real-font audit findings before -> after: 1366x768 14 -> 3, 940x620 16 -> 4, 1.6x text 62 -> 9 (what remains is by design: the editor scrolls long lines sideways, search rows are elided with the full text in a tooltip, and a 170-character scratch path cannot wrap) |
| 23 | Good-to-have features, chosen for real value to a learner (each one small, tested, and reversible) | DONE (first batch) | (a) **Snapshots you can restore**: Reset said "cannot be undone from inside the app" although it had just taken a copy nothing could read back. Now: an automatic daily snapshot 5 s after launch; retention 7 daily + 12 others counted apart (a week of dailies can no longer push out the pre-reset copy); Settings > Snapshots > "Restore a snapshot..." lists each with date, reason and contents (ticks / exercises / projects), restores through a scratch copy (an old schema is upgraded there, never in the backups folder) and snapshots the current state first, so a restore is undoable too; also in the File menu. tests/test_snapshots.py (16) + tests/test_snapshots_ui.py (9). (b) **Window size, position and maximised state remembered** (tests/test_window_geometry.py). (c) **Ctrl+/ comment toggle** in the editor at the block's shared indent, blank lines kept, one undo (tests/test_editor_commands.py). (d) **Help > Keyboard shortcuts (F1)** built from the live menu actions so it cannot drift, plus **Ctrl+,** for Settings; a test fails if two actions ever share a key (tests/test_shortcuts.py) |
| 25 | Bugs found while building 23 | DONE | (1) **Reset destroyed the learner's code for good**: one click next to Hint, via `setPlainText`, which also wipes the editor's undo history. Now one undoable edit, focus moves to the editor so Ctrl+Z reaches it, a toast says how; a no-op when the code is already the starter (tests/test_practice_reset.py, 2/2 mutants killed). (2) **A damaged database stayed locked after `Store()` failed**: the connection outlived the constructor, so on Windows the learner could not do what the error dialog told them ("move it aside") while it was open; now closed on failure (test in test_storage_hardening.py, mutant killed). (3) **Tab/Shift+Tab indented one line too many** after a whole-line drag (selection ending at column 0 of the next line); shared `_selected_lines()` fixes Tab and Ctrl+/ (mutant killed). Honest note: the `immutable=1` read of a snapshot survives its mutant (a plain read-only open of a checkpointed copy also leaves no trace); kept as the stricter never-write guarantee |
| 10b | Lint | DONE | `[tool.ruff]` in pyproject (E4/E7/E9/F + bugbear + RUF100 against stale noqa), ruff in the `dev` extra, `ruff check .` step in CI before the suite; the 11 + 6 findings fixed (a blind `pytest.raises(Exception)` now names `sqlite3.Error`; `zip(strict=True)` on the FSRS bounds) |
| 26 | Owner 09-22 (verbatim): "rework the ui research ui standards and any online data and any stuff and improve refine and make a proffesional ui and make a dark theme make it look premium and clean review and rework every nook and cranny till its literally perfection be harsh" | DONE | Research: WCAG 2.2 (1.4.3, 1.4.11, 2.4.11), Radix Colors 12-step scale, Material dark theme, Apple HIG Dark Mode/Color, Vercel Geist colours, Windows 11 spacing + typography, Emil Kowalski's design-engineering rules - written up with the token table in docs/UI-STANDARDS.md. Design system in ui/theme.py: 12-step neutral ladders for both themes (dark base #0F1114, not black; sidebar/card/hover/active as tonal steps; hairline vs element vs operable borders), one copper accent with hover/press/text/tint variants, semantic done/warn/bad each with text + tint, `on_accent` for text on any solid fill, focus and tooltip tokens; every pair run through a contrast solver (worst: 4.71:1 text, 3.51:1 control) and pinned by tests. Sheet rebuilt: 6/10/8 px radii, 4 px grid, tinted pills, tonal cards, thin scrollbars, floating menus/popups, visible focus on every control, arrows drawn in the theme's ink. Structure: one-line wordmark, inset rounded nav (active = surface step + weight; focus = dashed, Tab-only; first focus settled off the nav), a 520 px search field with a floating results panel, Today and Roadmap actions on the title row (145 -> 90 px cards), Phase jumps as real buttons, checked items strike prose but not code, chart empty cells and the editor gutter on the new grounds. Verified by eye on every page in both themes (real fonts) and by the audit harness: 1366x768 findings 14 -> 3 (all by design), 940x620 16 -> 4. Second pass (owner: "you dont need to stick to the orginal design or bland dashboards"): ui/widgets/icons.py draws a line icon per page and the app mark with QPainter (no icon font, same on every OS); the sidebar has a brand row, captioned sections and icons that follow the theme and the open page; Today is a hero (greeting + ui/widgets/ring.py progress ring), one stat strip, an "Up next" card on a tinted ground and a compact list for the rest; the Roadmap is a timeline (Rail widget: ticked / accent / numbered nodes on an unbroken line). Every attribute the walks read was kept; 113 UI tests green |
| 27 | Walk coverage was 47 of 73 controls (the coverage walk's 80 % floor) after the new dialogs and shortcuts landed | DONE | tests/walkthrough/test_walk_12_everything_else.py presses F1, Ctrl+,, the shortcut and snapshot dialogs (cancel, and a real restore), onboarding Back/Continue, the update dialog offline (failure path, button stays live), a quiz's score-page buttons, a concept card's Reveal + rating, Next exercise, resource/Library/project Open buttons and every project status button - dialogs driven through a patched exec so nothing blocks, URLs stubbed by the harness. Coverage matcher now knows StandardKey.Undo is the Ctrl+Z the walk sends and Ctrl+%d is Ctrl+1..9. The projects walk waits out the notes debounce instead of reading the store on the next tick |
| 28 | Changing the theme froze the window (3.8 s with every page built; 82 s for a run of the text-size spinner, 45 s for the switch after it) | DONE | Root cause measured, not guessed: `app.setStyleSheet` re-polishes every live widget at about 1.1 ms each, linearly (205 widgets 104 ms, 3,611 widgets 3.8 s; an empty sheet costs the same). Fix: `View.teardown()` drops the widgets of every page that is not on screen and not busy (a quiz, a review or a run in progress keeps its page; Phase keeps its open phase and flushes its note first, Practice saves its code, Projects flushes its notes), the deferred deletes are forced before the sheet is applied (deleteLater alone left them alive to be polished), the QPalette is applied only when the palette changed, and the spinner applies once, 250 ms after the last step. Measured: 0.40 s with every page previously built, 0.10 s after; the pages rebuild on the next visit as they always did on first visit. tests/test_theme_switch.py (6: cost, pages come back whole, the open phase survives, a quiz under way is kept, typed notes are saved first, the spinner applies once per burst) |
| 29 | Owner 09-22 (photo): blank white windows titled "Python Operator's Console" opening on every Start or click | DONE in source; the owner's exe is stale | The process on the machine is `%LOCALAPPDATA%\Programs\Operator's Console\operators-console.exe` dated 3 Sep - built before `discard()` (the hide-before-reparent fix for exactly these windows) and before everything in items 19-28. The source was proved clean on the real `windows` platform, not offscreen: a probe pressed every Start, opened every page, ticked, selected an exercise and switched the theme with the main window unshown, and counted zero stray top-level widgets. A fresh build was made with `python packaging/build.py --installer` -> `dist/operators-console-1.0.2-windows-setup.exe`; installing it is the owner's click, not mine |
| 30 | Walk findings after the theme fix (W-12, W-13, W-14, W-15 and five audit items at 1.6x text) | DONE | Redo was bound to Ctrl+Y twice (`setShortcuts([StandardKey.Redo, 'Ctrl+Y']]` collapses the standard key to its first binding) so Qt called it ambiguous and neither key worked -> the platform's bindings plus Ctrl+Shift+Z and Ctrl+Y, once each; the practice autosave fired 700 ms after a quick quit against the closed store -> closeEvent stops every timer under the window after the flushes; a long status message widened the whole window (1225 px in a 1024 px window) -> `ElidedLabel` paints an ellipsis and keeps the text whole for readers and the tooltip; the search field shrank to 194 px at 1.6x -> two shares of the slack and a scaled minimum; phase names elided in the stats table -> the table asks for its widest name; the phase picker elided '11 Async, concurrency & performance' -> sized to contents; Library redrew its certificates on every visit -> skipped when the store is unchanged; pages lost their right edge instead of scrolling sideways at 1.6x in 1024 px -> the Scroller scrolls sideways when it must (never at a supported size). The walk itself kept every closed window alive, so its theme timing read 19 s where the app takes 0.5 s -> windows are destroyed on teardown; its 'Projects and Today count different totals' was a stale message (both count the plan) and 'the Hint button is dead' described a button that is now hidden. tests/test_close_and_shortcuts.py (4) |
| 31 | A page switch cost 34 ms (the walk's 200-switch stress step froze for 6.7 s) | DONE | Profiled, not guessed: Phase rebuilt its picker, jump row and every check row on each visit, Today and Quiz redrew, Journal refilled its history, the status line recomputed the whole overview (33 quiz-score queries), and all eleven nav buttons were re-polished each time. Now each of those skips while the store (and the clock bucket it depends on) is where it was - `View.store_unchanged` / `mark_drawn`, a shared `store_key()` - and only the two nav buttons that change are re-polished. 16 ms a switch, paint included; tests/test_page_switch.py (4). The walk's coverage test ran as file 11, before file 12 pressed its buttons, so it under-counted every run - it is file 13 now, and the inventory reads `setShortcuts(_redo_keys())` |
| 32 | Owner 09-23 (verbatim): "even on running app when a new release is released it should appear too" | DONE | The manager looked once, 2.5 s after launch, and never again, so a window left open for days saw nothing until a relaunch. `UpdateManager` now also polls every 30 minutes while the window is open (two requests an hour, inside GitHub's unauthenticated limit); the poll skips the once-a-day gate but honours the opt-out and the source-checkout guard; a release found mid-session goes on the sidebar button with a toast and never takes the keyboard away; the same release is announced once, a newer one replaces it; the timer dies with the window. Settings copy says so. tests/test_update_poll.py (6) |
| 33 | Owner 09-23 (verbatim): "weird broken highlight that goes on anything need to be removed" | DONE | It was never a highlight. Qt has no `:focus-visible`, so every `:focus` rule fired however the focus arrived - a mouse press, or whatever Qt parked the focus on after a page was rebuilt - and the nav ring was dashed, which reads as damage. New `ui/focus.py` installs one application event filter that marks a widget `kbd` when its focus came from Tab, Shift+Tab or a shortcut; every ring selector in theme.py is now `[kbd="true"]:focus` and none is dashed. Text fields keep their plain `:focus` border, which is the documented `:focus-visible` exception: clicking into a field is how you start typing. Reported the same day: the checked checkbox drew Qt's stock `standardbutton-apply-16.png`, a green plate that covered the box, ignored the theme and matched nothing else - `theme._check_image` draws the app's own tick in `on_accent`. A folded group ("4 more to study") now carries an OPTIONAL pill (`Disclosure(tag=...)`); the Projects fold passes `tag=""` because its caption already says it. tests/test_focus_visible.py (8) |
| 34 | Owner 09-23 (verbatim): "my options were not saved" | DONE - cause found, guard added | Read the owner's store read-only: `goals` held all nine ids and `started_on` was "2026-09-03", the old build's date. They were running TWO copies at once (the 3 Sep install and the new one) over one SQLite file, so settings were last-writer-wins and the stale copy clobbered the new one. `app.py` now hands over to the running copy through a QLocalSocket keyed on a hash of the data dir (a portable copy with its own data dir still runs alongside), raises that window and exits; every path falls back to carrying on alone if the socket machinery fails, and the update helper and the exercise runner are dispatched before it. The settings round trip is now pinned end to end (goals, track, experience, name, pace, theme, text size, through a teardown and rebuild). tests/test_single_instance.py (11) + tests/test_gap_settings.py (23) |
| 35 | Owner 09-23 (verbatim): "when testing there should be proper failure for each and why and reasosn and explained depending on how he failed do that in everything" | DONE | Every failing check said `Wrong result.` because `runner.py` execs each check and a bare `assert` raises with an empty message. Now, inside the child process where the values still exist: `explain_assertion` walks the traceback to the failing `<check>` frame, finds that line's `ast.Assert`, and re-evaluates both operands of the comparison in that frame; `describe_difference` adds the reason - returned None, wrong type, wrong length, right values wrong order, differs at index N (recursing one level), dict missing / extra / wrong value, set difference, strings (case-only, whitespace-only, stops early, runs on, first differing character with a caret), floats close but unequal, bool vs int. Bare `assert f(x)`, `in`, `isinstance`, ordering and author-written messages all handled; a re-evaluation that raises degrades to the plain message. `explain_error` keeps `TypeError: ...` and adds a hint for argument count, NameError, AttributeError on None, IndexError, KeyError, ZeroDivisionError, RecursionError, unhashable, non-callable. Values are reprlib-capped, no path can leak, and the explanation is computed inside the output capture. `CaseResult.detail` carries the values under the sentence. tests/test_grader_messages.py (64); all 96 reference solutions still pass |
| 36 | Owner 09-23 (verbatim): "teh onboarding is ugly" | DONE | The wizard was four bare form steps with two inline stylesheets. Rebuilt: four progress segments, every step inside a Scroller (so 1.6x text cannot clip it), experience and goals as selectable cards that take the accent tint when chosen (one QButtonGroup, so arrow keys work across separate parents), the track preview and pace estimate as focus cards, the field sized from its font metrics, Enter = Continue. Found on the way: Escape never actually skipped - it closed without writing `onboarded`. The wizard also prefills from the stored answers now, without which "Run setup again" would wipe goals on the last press - item 34's bug all over again |
| 37 | The learner-view gap sweep: 25 gaps and 16 polish items from a fresh-eyes audit of all eleven pages, built in six parallel streams | DONE | **Practice:** stop a running exercise (the job kills the child process tree); the hint ladder clamped, hints stacked, "2 hints used" recorded; "solved after reading" as a status, a filter and a report line; the last passing code kept and restorable; a text and difficulty filter over 96 exercises; Next disabled on the last; the hint button disabled rather than vanishing; "attempted" instead of "IN PROGRESS". **Review:** Space/Enter, A-D, 1-4 and S bound and printed on the buttons, with an "In review" table in F1; bury a card and restore it (this found a real backend bug - `suspend_card` was an UPDATE, so burying a card nobody had answered did nothing; it upserts now); undo a rating, which also unwinds the day's review count; a second skip drops the card instead of looping; CheckRow is keyboard-reachable with Shift+F10, the Menu key and a lazily built "..." handle. **Today / Roadmap / Progress / Phase:** a finished-curriculum state (hero with dates, hours and longest streak, Export report / Keep reviewing / Change track, a maintenance plan, a roadmap banner instead of a stale "YOU ARE HERE"); yesterday's "first thing tomorrow" resurfaces as the day's first action; "Not today" per row with "Show N hidden"; "1.5 h of your 3 h logged today"; Progress rows open their phase by click or Enter and carry a quiz trend; the notes box grows. **Log:** filter, range and "Show older" paging; edit an entry as one undoable step; undoable delete that restores the day's minutes; "You already logged 2.5 h today - this adds to it"; every note you have written, in one place, with an Open button. **Search / Library / shell:** notes, project notes, repo URLs and log entries are searchable and rank above curriculum lines, cached on the store's change counter; Down/Up/Enter/Escape in the results; a certificate or field hit lands on its card and flashes it; a question hit opens a read-only card instead of starting a quiz; Library read marks (through the undoable check API) and a filter; toasts raise a screen-reader alert; Ctrl+0 and Ctrl+L; a one-time pointer to the guide. **Settings / Quiz / Projects:** leave and resume a quiz, "Skip (counts as wrong)", run setup again, the update opt-in at first run, a second backup folder with "Last export", before-and-after summaries on restore and import, Open disabled without a URL, README corrected. About 200 new tests across tests/test_gap_*.py, test_grader_messages.py, test_single_instance.py and six new walk files |
| 38 | A theme change still froze for 2.5 s once every page had been built | DONE | Item 28 stopped the sheet from polishing three thousand widgets, but the teardown still retired them one at a time from Python - `clear_layout` walks the tree and hides, reparents and `deleteLater`s each widget, a Python round trip each. `Scroller.reset()` hands the scroll area a fresh body instead and lets Qt delete the old tree in C++ (`setWidget` deletes what it replaces; the old body is hidden first so it can never flash as a window). Measured with every page built: 3,460 widgets, flip 2.51 s -> **0.69 s**, and the census drops to 235; the pages rebuild whole on their next visit (3,423). The second flip is 0.47 s |
| 24 | Walk the whole app as a learner after all of the above (`pytest -m walk` + a real-font pass by hand) and record every problem | DONE - 82 of 82, 1,258 steps, 0 exceptions | docs/WALKTHROUGH.md, 2026-09-22 21:30. From 15 findings to 3: the 200-switch stress step (3.3 s for 200 switches, 16 ms each - a stress figure, not a freeze a learner meets), a second journal entry on the same day is kept separate on purpose, and exercises that ship no hint (content). Coverage 71 of 73 controls (97 %); the two not pressed are a help-menu shortcut read from a table and a certificate's Open link. Worst theme switch 582 ms with every page built (was 40 s). Real-font audit after the fixes: 940x620 13, 1366x768 at 1.6x text 13, 1024x640 at 1.6x 32 (was 62) - every remaining item is by design and listed in docs/UI-STANDARDS.md |

## Owner's request 2026-09-23 (verbatim)
"do all of them thoroughly harshly audited 10/10 i want all the users with current old version to be able to update form withing the app they have and it should work too, also do anything that missing improve everthing and do you best to maximize learning an dmake it most accurate etc if you need to add more quizzes verify data make sure filters work etcc"

| # | Item | Status | Evidence |
|---|---|---|---|
| 39 | Installed 1.0.0-1.0.2 builds could not update in the app: their helper runs as `<install>\operators-console.exe` and runs the new installer while holding that file, so Inno Setup aborted (exit 5, "unable to automatically close all applications ... Defaulting to Abort") and the old version reopened. The learner's own `%APPDATA%\Operator's Console\update.log` shows two such attempts against the published v1.0.2 installer. Old code cannot be patched, so the fix lives in the new installer | DONE (proven on real installs) | installer.iss: `CloseApplications=force`, `CloseApplicationsFilter=*.exe,*.dll,*.pyd`, `RestartApplications=no`, `SetupLogging=yes`, and one `[Run]` entry gated by `RelaunchAfterUpdate` = `WizardSilent and not CmdLineHas('/NORELAUNCH')`; 1.1.0+ helpers pass `/NORELAUNCH`. `packaging/windows/check_old_build_update.py`: with the old installer the update aborts and the old version reopens; with the fixed one the old build ends up running the new one. The stale test that pinned "never start on a silent run" rewritten to the real contract, plus a test for the close settings (tests/test_update_restart.py) |
| 40 | 1.0.x PORTABLE Windows builds wreck their own folder when they update (their `_apply_archive` deletes parts of the running `_internal`). Nothing on their machine can be changed | DONE (from the release side) | the Windows portable zip is now `operators-console-<v>-windows-x64-portable.zip`, which does not contain the old needle `windows-portable.zip`: an old portable build reports "no download for your platform" instead. Installed builds still match `find("windows", "setup.exe")`, name unchanged. build.py, the release notes, the current picker and install.ps1 use the new name; the picker and install.ps1 also still find the old one. tests/test_release_shape.py reproduces the 7ad8750 picker and pins both |
| 41 | The release job published whatever it was handed, build.py skipped the installer silently without iscc, and a tag on the wrong commit would misname every file | DONE | `python packaging/build.py --check-release artifacts --tag $GITHUB_REF_NAME` runs before anything is published: every expected file by exact name, no file under the old portable name, tag == `v` + version.py. `SHA256SUMS` is generated, its line count checked against the files and verified with `sha256sum -c`; `fail_on_unmatched_files`, not draft, not prerelease, latest. `--installer` now stops with an error when iscc is missing or produces nothing. tests/test_release_shape.py |
| 42 | Two copies started together after an old build's update (the installer's relaunch and the old helper's) both found nobody on the socket, because the first copy did not listen until its window was built, and both opened on one database; also, every failed update left its package in the data folder for ever (a live 23 MB orphan) | DONE | app.py: `claim_instance` takes a `QLockFile` in the data folder and listens at once, before the database is opened; a copy that finds the lock held waits for the owner's socket and hands over (never stale by age; a dead owner's lock is taken over). Proven with two real processes released at the same instant and 1.5 s apart - one OWNER, one HANDED, the owner raised once - and a crashed owner's lock not blocking the next launch (tests/test_update_restart.py). updates.py: `tidy_staging()` also clears packages, `.part` files and `.sha256` sidecars older than the grace period (never the log, a package being handed over, or the running helper's own folder) and runs on every failure path of `apply_update` (tests/test_updates.py) |
| 43 | Documentation behind the code: README described a four-item undo, a five-row keyboard table, 120 tests and an old layout; the release body said "the app makes no network calls"; there was no CHANGELOG; install.ps1 said "look in your Start menu" while the installer had already reopened the app; pyproject pointed at the wrong repository; grader scratch files in the repository root | DONE | README rewritten against the code (counts pinned against the shipped data by a test), CHANGELOG.md for 1.1.0 (why 1.1.0: schema version 1 unchanged), .github/RELEASE_NOTES.md opens with the 1.0.x note and states the one network request, pyproject URLs, `.gitignore` covers the stray output; screenshots are being re-shot as today-light/-dark, roadmap-light, practice-light/-dark and review-light |
| 44 | Learning, by parallel agents on 09-23: phases count as finished when proven (gate, quiz at 85%, 60% of exercises, a project), gate checks as recall cards, the quiz bank rebalanced (163 of 167 answers were option B) with per-choice feedback and new questions, new exercises, every filter exercised on the real data | See each agent's report | proof-based progress (`core/progress.py` `is_proven`) and gate cards (`core/review.py`) were on disk when row 43 was written; the rest is recorded by the agents that did it |
| 45 | Owner 09-23 (verbatim): "make sure optional and main are actually correct you need to decide whta sactually optional to be hidden and whats main , also when updates reease they have to be available in the ui immediately, also the options on hover collapse lower as in the picture" | DONE (unreleased) | Hover: CheckRow's lazily built `...` handle was taller than a text line and the label was centred, so rows grew 22 -> 23 px under the mouse and the text dropped; the handle is now fixed at `line_height()` and the label is top-aligned (real-font render: 22 px before and after hover, box and text tops equal). Main/optional: four stretch sections marked optional at the source (p05 Challenge ladder, p18 Leverage, p18 Staying current, p99 Portfolio target), folded behind OPTIONAL and excluded from progress; three primary picks corrected (p05 NeetCode, p16 CS50 week 4, p99 Build your own X). Updates: every launch looks (the once-a-day gate is gone), POLL_MINUTES 30 -> 5, a look on returning to the window (throttled to once a minute), ETag/If-None-Match so unchanged answers are free 304s. tests/test_checkrow_layout.py, test_optional_sections.py, test_update_freshness.py |
| 46 | Owner 09-23: quiz results "ugly", "took is really bad", countdown per question with lost marks when late, "what to look at again" must find exactly what to study with ways to go there, questions and answers reshuffled every attempt and never the same organization, everywhere in the app; exercise prompts should say what they want and hints be written better | DONE (unreleased) | core/quiz_session.py (question_seconds, fresh_order, fresh_choice_order, format_duration, study_plan); quiz view rebuilt (countdown with amber/red bar, late = no mark + Hard rating, persisted; lettered choices; score card; What to study with Open this line / Practise / Read; Question by question; practice round of misses); review cards and session shuffled with the same never-repeat rule; 121 questions mapped to their teaching line; core/load_errors.py plain-English load errors; core/exercise_brief.py examples + how it is checked + shape hint; 72 phase-01 hints rewritten (build_tools/hint_rewrites.json). tests/test_quiz_countdown.py, test_exercise_clarity.py |
| 47 | Owner 09-24: "the wording sounds ai ish be direct refine every wording in the app be direct no fluff and be flat and straightforward" | DONE (unreleased) | Every prose string in src/ inventoried (AST scan, ~400); ~200 rewritten flat by exact-literal replacement with placeholder order checked; page kickers made plain; phase aims, gate notes, field/cert/channel/resource notes, track blurbs and project text rewritten at the source (build_tools/raw_curriculum.json, project_edits.json); p01 hints and six prompts (hint_rewrites.json, prompt_edits.json); README intro and How it works flattened; stale '30 minutes' onboarding text fixed. Quiz explanations kept: technical and direct. |
| 48 | Owner 09-24 (verbatim): "make sure everthing you want to learn it specilaizes teh circicullum and organizes it based on your choices ... make sure this app has every python ptahways ... help you reach it senior to best level possible" | DONE (unreleased) | build_tools/specializations.json (S1-S14), spec_quizzes.json (112 questions, fairness rules met: 0 padded, 27% longest), ex_spec.py (42 exercises, verify_exercises 161/161), proj_d.py (14 projects); transform.py PREREQ graph replaces the straight spine and fails the build on any phase missing from EST/PREREQ/TAGS/LEVEL; core/adaptive.py 21 goals, `plan_order`, `with_prerequisites`; tracks.json 16 tracks. tests/test_progress_edges.py `test_the_roadmap_puts_every_phase_after_its_prerequisites`. Found on the way: every ticked box recomputed all 35 phases twice, reading the store once per phase; `Progress.all_phases` now reads the store once per pass and is reused until the store is written to (`test_the_phase_pass_is_reused_until_the_store_changes`). The walkthrough's exhaustive goal test would have been 2**21 previews; it now covers none, all, every single goal, every pair and 512 seeded patterns. Specializations are held to the exercise authoring contract (tests/test_exercise_coverage.py) |
| 49 | Owner 09-24: "learn everthing about coding proffesionally with senior + level ... showing them that they made progress" | DONE (unreleased) | core/progress.py `Career`, `LEVELS`, `Progress.career()` from proven phases; ui/widgets/career.py on Today and Progress; level-up toast once (`career_level` setting). tests/test_feedback_and_career.py climbs to Senior+ |
| 50 | Owner 09-24 (verbatim): "ADD USER CREATION IF THEY WANT TO SWITCH USERS ETC , AND EACH USER HAS HIS OWN DATA MAKE IT SMOOTH AND CLEAN AND ALSO ADD REPORT / REQUEST FORM IN SETTINGS IF THEY NEED SOMETHING OR REPORT A BUG" | DONE (unreleased) | core/paths.py root vs profile folder; core/profiles.py; app.py `profile_switcher` (in-process, falls back to the previous profile on failure); File menu + Settings Profiles card; core/feedback.py + ui/feedback.py (prefilled GitHub issue, nothing sent by the app). tests/test_profiles.py (13), tests/test_feedback_and_career.py |
